// TODO: ideally I'd import like this, but I get this error:
// Cannot find module 'shared-constructs/MyConstruct' or its corresponding type declarations.ts(2307)
// import { MyConstruct } from "shared-constructs/MyConstruct";

// This works, though.
import { MyConstruct } from "shared-constructs/src/MyConstruct";
